{#
/**
    The 'Parent' template for the main menu.
 */
#}

{% set heading_id = attributes.id ~ '-menu'|clean_id %}
<nav class="footer container" role="navigation" aria-labelledby="{{ heading_id }}"{{ attributes|without('role', 'aria-labelledby') }}>
  {# Label. If not displayed, we still provide it for screen readers. #}
  {% if not configuration.label_display %}
    {% set title_attributes = title_attributes.addClass('visually-hidden') %}
  {% endif %}
  {{ title_prefix }}
  <h2{{ title_attributes.setAttribute('id', heading_id) }}>{{ configuration.label }}</h2>
  {{ title_suffix }}

  {# Menu. #}
  {% block content %}
    {{ content }}
  {% endblock %}

  <p>© 2022 VICE MEDIA GROUP</p>

  <div id="myDIV" class="modal mystyle container">

    <div class="navigation-burger">
      <div class="burger" id="burgerMenuClose">
        <div class="line"></div>
        <div class="line"></div>
      </div>
      <div class="title">
        <p>Menu</p>
      </div>
    </div>

    <a href="/"><img src="{{ directory ~ '/vice_logo.png' }}" /></a>

    <div class="menu">
      <div class="menu-links">
        <a href="/environment"><h3>Environment</h3></a>
        <a href="/music"><h3>Music</h3></a>
        <a href="/world-news"><h3>World News</h3></a>
      </div>
    </div>
  </div>

</nav>